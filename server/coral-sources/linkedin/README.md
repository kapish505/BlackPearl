# LinkedIn Coral Source

This is a custom Coral source spec for LinkedIn. It exposes the following tables:
- `profile`: Maps to `/v2/userinfo`. Use this table to fetch your LinkedIn user profile information.

This source uses the `http` backend to connect to `https://api.linkedin.com/`.
